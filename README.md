# Video Downloader 🎵

A Flask web application to download audio from YouTube and other sites.

## Setup
1. Install FFmpeg on your system.
2. Install requirements: `pip install -r requirements.txt`
3. Run: `python app.py`
